#  Copyright (c) 2026 Metaform Systems, Inc
#  SPDX-License-Identifier: Apache-2.0

apiVersion: v1
kind: ConfigMap
metadata:
  name: jwtlet-config
  namespace: vault-e2e-test
data:
  jwtlet.toml: |
    issuer = "http://jwtlet.vault-e2e-test.svc.cluster.local"

    [storage_backend]
    type = "postgres"
    url  = "postgresql://jwtlet:jwtlet@postgres:5432/jwtlet"

    [k8s]
    api_server_url  = "https://kubernetes.default.svc"
    cluster_issuer  = "https://kubernetes.default.svc.cluster.local"

    [token]
    client_audience = "https://kubernetes.default.svc.cluster.local"
    audience        = "jwtlet-e2e"

    [vault]
    url        = "http://vault:8200"
    key_name   = "signing-jwtlet_pc"
    token_file = "/vault/secrets/.vault-token"

    [service_accounts]
    "system:serviceaccount:vault-e2e-test:test-app-sa" = ["jwtlet:management:mappings:write", "jwtlet:management:scope:write"]
